# Rà toàn dự án 2026-09-09 — AI · gameplay · map · animation · vũ khí

> User: *"Kiểm tra hết tất cả AI, gameplay, xây map, animation, hệ thống weapon xem cải thiện,
> thêm tính năng và sửa lỗi toàn vẹn."* Cách làm: **quét bằng máy theo 13 lớp lỗi có tiền sử**
> trong dự án (902 file C#), phân loại thật/giả từng lớp, sửa thứ an toàn, và biến lớp lỗi mới
> thành phép đo Doctor để nó không quay lại. Không mở Unity; mọi số dưới đây đo từ văn bản
> nguồn và tài nguyên trên đĩa.

## 0. Bảng tổng — 13 lớp lỗi, cái nào thật

| Lớp lỗi quét | Số điểm | Kết luận | Xử lý |
|---|---:|---|---|
| `??`/`?.` trên đối tượng Unity (fake-null) | 23 | **13 thật**, 10 giả (`FortPieceDef`/`MapLotDef` là class thường) | **sửa 10**, 3 ở file sổ nợ đang được phiên khác sửa → để lại |
| Tra `GetComponent`/cấp phát mỗi khung trong `Update` | 8 | **2 thật** (`MapWrapZone` ×3/người/khung, `LootTierMark` ×2/món/khung); 6 giả (có nhịp, cắm-sau có chủ ý, `Awake`) | **sửa 2** |
| Class runtime không ai gắn | 8 | **1 thật**: `HealthProfileSwitcher` — 0 code, 0 prefab, 0 scene; 7 giả (bootstrap, extension, `WorldThing` cố ý) | báo; phép đo mới |
| Mode không vào được công thức JSON | 4 | **4 thật** | `AppleShot`/`ArcheryTrial` → `NotInRecipe` kèm lý do; 2 mode võ lâm đang do phiên khác viết |
| Giá trị enum không ai dùng (17 enum) | 1 | `StickmanActionType.Swim` | báo; phép đo mới |
| Ghi `Time.timeScale` | 45 | đều có đường trả (HitStop · saved/restore · scene reload) | không |
| `UnityEngine.Random` ở Map/Gameplay | 27 file | `MapGenerator` dùng `InitState(seed)` + trả state → hợp lệ; 26 file gameplay còn lại là ngẫu nhiên hành vi, ngoài hợp đồng seed | báo |
| Event tĩnh đăng ký > huỷ | 2 | giả — là CHUỖI MẪU trong Doctor | không |
| API cũ (`velocity`, `FindObjectOfType`, `GetInstanceID`) | 2 | giả (`launch.velocity` là struct; chuỗi mẫu) | không |
| MonoBehaviour trong file khác tên | 13 | không cái nào bị prefab/scene tham chiếu (hit `PoolManager` là chuỗi con của `ProjectilePoolManager`) | không (luật cấm dời class) |
| `catch {}` rỗng | 8 | đều có chú thích chủ ý | không |
| `LoadScene` trong `OnGUI` | 0 | | |
| SpriteSkin `autoRebind: 1` | 0 | | |

## 1. Theo từng hệ

### AI
- 15 `AIBehavior`, 17 file `AIState*`, 14 module — **mọi state và module đều có người gọi**
  (phép đo class-không-ai-gắn không réo cái nào).
- `HealthProfileSwitcher` (đổi `AIProfile` theo % máu cho lính thường) là tính năng **viết
  xong không gắn**: cần số ngưỡng + profile trong Inspector nên không gắn bừa được. Việc tiếp:
  builder nào muốn lính "cuồng máu" thì `AddComponent` + điền bảng, hoặc xoá.
- `FantasyAirRaider`: `FindNearestEnemy(...)?.Controller` — sửa (fake-null).

### Gameplay
- 35 kiểu chơi, **không mode nào thiếu `Finish/Win/Lose`** (không có "trận trôi mãi").
- `TeamEconomy`: hai chỗ `?.` sửa **không tăng dòng** (file trong sổ nợ) nhờ overload mới
  `UnitRankGear.Attach(character, pinTier)`.
- `StructureKitShowcase`, `MapPrisonCamp`, `TeamMember`, `FantasyCreatureLook`: 8 chỗ `?.`/`??` sửa.
- `AppleShotMode`, `ArcheryTrialMode` cố ý ngoài công thức (đạo cụ và quỹ đạo, không phải mốc
  trên đất) — đã khai `NotInRecipe` để Doctor hết đỏ đúng chỗ.

### Xây map
- Kho map **71 map, 0 map mồ côi**, mọi map đều khai thể loại. Bảng phủ kiểu × thể loại:

| Kiểu | Med | Mod | Fan | Wux | Ghi chú |
|---|:-:|:-:|:-:|:-:|---|
| Annihilation…CaptureFlag (8 kiểu) | ✔ | ✔ | ✔ | ✔ | |
| TugOfWar · RaidVillage · SiegeStarve · RescuePrisoners · ChampionDuel · WarEconomy | ✔ | ✔ | ✔ | **✘** | võ lâm thiếu 6 kiểu — gói *Đường về Nhạn Môn* chặng ChampionDuel đang **mượn map trung cổ** |
| BattleRoyale | ✔ | ✔ | ✔ | ✘ | |
| ZombieHorde · ZombieConvoy | ✔ | ✘ | ✘ | ✘ | zombie chỉ có ở TRUNG CỔ, dù gói *Đêm thứ bảy* khai thể loại Modern → mượn map trung cổ |
| **DragonHunt · ArcaneRift** | ✘ | ✘ | ✘ | ✘ | **không có map nào** — mọi lời xin rơi về ngẫu nhiên |
| OpenWorldCity · TeamDeathmatch · DungeonDelve · BossLair (mới) | ✔ | một phần | ✘ | ✘ | phiên khác đang làm |

- `MapWrapZone`: đảo thứ tự kiểm tra (so x trước, hỏi component sau) — bỏ 3 `GetComponent`
  cho mọi người mỗi khung, **không cache** để không dính bẫy hỏi-một-lần với `IPinnedInPlace`.
- `MapAssembler` (3 chỗ `??` trên `Transform`) và `StructureAssembler` (2 chỗ) **để lại**: cả hai
  đang được phiên khác sửa (đã +232 / +55 dòng so với sổ nợ trong ngày), vá chồng là đụng nhau;
  cả 5 chỗ đều vô hại lúc chạy (`Find` trả null hoặc object sống). Phép đo `??/?.` mở rộng đã
  bắt được chúng — sẽ hiện đỏ để chủ file sửa.

### Animation
- SpriteSkin: **0** asset bật `autoRebind` (luật rig giữ được).
- `StickmanActionType.Swim` là động tác **không ai phát**: hoặc nối (bơi qua nước) hoặc bỏ.
- Không có `MonoBehaviour` animation nào nằm sai file.

### Vũ khí
- `LeafWeaponPaths` (71 cây) là **không gian chỉ số của `StickmanWeaponHolder`**, không khoá
  theo enum `WeaponType` (74 giá trị) — phép đo ban đầu của tôi giả định sai, đã bỏ. Builder đã
  có phép đo đường dẫn ↔ đĩa (`StickmanWeaponBuilder.Giao.cs`). 9 prefab ngoài bảng là chủ ý
  (`Weapon_Base`, vũ khí xe/máy bay).
- Mọi giá trị `WeaponType`, `WeaponClass`, `AimMode`, `MeleeStyle`, `GearRule` đều có người dùng.

## 2. Phép đo Doctor thêm (`StickmanDoctor.Wiring.cs` + mở rộng `Invariants`)

| Phép đo | Canh gì |
|---|---|
| **Class runtime: có ai gắn không** | ba nguồn: chữ trong .cs khác · GUID script trong .prefab/.unity/.asset · `m_EditorClassIdentifier`; nợ cố ý ghi ở `KnownUnwired` kèm lý do |
| Giá trị enum: có ai dùng không | 14 enum then chốt; báo vàng |
| Kiểu nhiệm vụ: kho map có màn không | đỏ khi không có map NÀO; vàng khi thiếu một thể loại |
| Update: có tra component mỗi khung không | bỏ qua vòng có nhịp và cắm-sau `if (_x == null)` |
| `??`/`?.` (mở rộng) | thêm `FindAnyObjectByType` · `transform.Find` · `FindNearestEnemy` — 5 chỗ từng lọt lưới |

## 3. Việc còn lại, và của ai

| Việc | Ai / khi nào |
|---|---|
| Dựng map `DragonHunt`, `ArcaneRift` (Fantasy), 6 kiểu cho Võ lâm | «Xưởng map» — cần Unity |
| 5 chỗ `??` ở `MapAssembler` / `StructureAssembler` | chủ phiên đang sửa hai file đó; Doctor sẽ réo |
| `WuxiaTournamentMode`, `WuxiaTrainingMode` vào công thức hoặc `NotInRecipe` | phiên đang viết hai mode |
| `HealthProfileSwitcher`: gắn hoặc xoá | quyết định thiết kế |
| `Swim`: nối hoặc bỏ | quyết định thiết kế |
| Chạy «★ KHÁM SỨC KHOẺ» trong Unity để xem 4 phép đo mới trên dữ liệu thật | lần mở Unity tới |
